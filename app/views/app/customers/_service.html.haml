= form_with(model: customer) do |form|
  - if customer.errors.any?
    #error_explanation  
      - customer.errors.full_messages.each do |message|
        .col
          .alert.alert-danger.text-white
            = message
  .card
    .card-header
      %h1.header-title
        Service
    .card-body 
      .row
        
    -# .card-footer
    -#   .actions
    -#     = form.submit "Select Service", class: "btn btn-primary"         